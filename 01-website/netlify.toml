# Separate website project only. The repository root still deploys the live web app.
[build]
  base = "01-website"
  command = "npm ci && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
  ALLOW_INDEXING = "false"
